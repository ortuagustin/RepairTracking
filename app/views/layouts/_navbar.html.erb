<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">
    <span class="pl-2">
      <i class="fas fa-wrench"></i>
      <span class="ml-1">Repair Tracking</span>
    </span>
  </a>

  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto"></ul>

    <% if user_signed_in? %>
      <span>
        <span class="navbar-text">
          <div class="col">
            <%= t 'navigation.welcome', email: current_user.email %>
          </div>
        </span>

        <div class="col text-center">
            <logout-button
              url="<%= destroy_user_session_path %>"
              title="<%= t 'navigation.actions.logout.button' %>"
              type="<%= 'warning' %>"
              confirm-button="<%= t 'navigation.actions.logout.confirm' %>"
              cancel-button="<%= t 'navigation.actions.logout.cancel' %>"
            >
              <%= (t 'navigation.logout_link_label') %>

            </logout-button>

        </div>
      </span>
    <% end %>
  </div>
</nav>